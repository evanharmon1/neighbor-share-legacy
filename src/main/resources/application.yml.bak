
---
en-US:
  prop:
    spring:
      thymeleaf:
        cache: "false"
      datasource:
        url: jdbc:mysql://localhost:8889/neighbor-share?useLegacyDatetimeCode=false&serverTimezone=UTC
        username: neighbor-share
        password: neighbor
      jpa:
        database: MYSQL
        show-sql: "true"
        hibernate:
          ddl-auto: update
        properties:
          hibernate:
            dialect: org.hibernate.dialect.MySQL5Dialect
            enable_lazy_load_no_trans: "true"
...
